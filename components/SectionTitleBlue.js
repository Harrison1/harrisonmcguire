const SectionTitleBlue = ({ title }) =>
    <div>
        <h2>{ title }</h2>
        <style jsx>{`
            h2 {
                color: #00bcd4;
                font-size: 4rem;
                text-align: center;
            }
        `}</style>
    </div>

export default SectionTitleBlue